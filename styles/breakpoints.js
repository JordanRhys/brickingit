const size = {
  sm: '400px',
  md: '700px',
  mdlg: '800px',
  lg: '1000px'
}

export const breakpoints = {
  sm: `(min-width: ${size.sm})`,
  md: `(min-width: ${size.md})`,
  mdlg: `(min-width: ${size.mdlg})`,
  lg: `(min-width: ${size.lg})`,
}
